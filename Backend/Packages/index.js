const catme = require('cat-me');

console.log(catme());

//cat-me is a package that generates random cat images and facts.
// It can be used to add some fun and entertainment to your projects.
// You can install it using npm by running the command "npm install cat-me".
// Once installed, you can require it in your code and call the function to get a random cat image or fact.